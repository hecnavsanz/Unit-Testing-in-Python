#!/usr/bin/env bash

# create data-formatter project directories
mkdir -p "$HOME"/data-formatter
mkdir -p "$HOME"/data-formatter/modules
mkdir -p "$HOME"/data-formatter/tests
cd "$HOME"/data-formatter || exit
pwd
ls -l
