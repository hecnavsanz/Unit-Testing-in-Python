# non-default test directory and files naming convention
[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-s -vv"
testpaths = ["blog-tests"]
python_files = ["*-test-case-*.py"]
pythonpath = ["/home/labs/blog"]
